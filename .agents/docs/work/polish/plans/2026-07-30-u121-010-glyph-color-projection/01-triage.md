---
title: U121-010 plan — GitHub triage
type: plan
status: active
parent: "[[docs/work/polish/plans/2026-07-30-u121-010-glyph-color-projection/index|U121-010 plan]]"
created: 2026-07-30T00:00:00
updated: 2026-07-30T00:00:00
created_by: codex-gpt5-root
updated_by: codex-gpt5-root
tags:
  - agent/plan
  - initiative/polish
  - release/1.2.1
  - glyph-color
---

# GitHub triage

### Task 1: Correct and triage GitHub #46

**Files:** None in product.

- [ ] **Step 1: Re-read issue #46 and confirm it has no newer comments**

Use the GitHub connector for `Meibbo/Vaultman#46`. Expected: the stale body
still claims Table/Cards receive no projection and comments are empty.

- [ ] **Step 2: Replace the issue body with the approved contract**

The replacement body must start with:

```markdown
> *This was generated by AI during triage.*
```

It must identify the confirmed bug, Stream/Goal distinction, exact scope
matrix, Iconic/name precedence, ten-slot distribution, tests, gates, HITL
matrix, and explicit non-goals from the approved spec. Do not introduce or
change any semantic color variable outside the approved Cell projection.

- [ ] **Step 3: Apply one category and one state role**

Use category `bug`. Replace `needs-triage` with `ready-for-agent` if the label
exists. If the repository lacks the state label, create it with description
`Fully specified and ready for an AFK agent`, then apply exactly
`bug` + `ready-for-agent`.

- [ ] **Step 4: Post the durable agent brief**

The comment must begin with the AI-triage disclaimer and include:

```markdown
## Agent brief

**Base:** `main` 1.2.0; implementation worktree
`C:/tmp/vaultman-release-beta2-final2`.

**Confirmed causes:**
- `_refreshFolderIcon()` drops the glyph fallback after open/close icon refresh.
- Tree uses an eight-slot global DFS bucket instead of ten-slot root-position
  distribution with branch inheritance.
- Table and Cards have no file glyph-color callback.

**Required paths:** `logicGlyphColor.ts`, `explorerFiles.ts`, `viewTree.ts`,
`viewGrid.ts`, `viewFilesGrid.ts`, and focused tests.

**Do not change:** settings/persistence, Floating Index product code,
virtualization utilities, active-link semantics, or U121-013 generic layers.
```

Expected: issue remains open and is ready for implementation.
