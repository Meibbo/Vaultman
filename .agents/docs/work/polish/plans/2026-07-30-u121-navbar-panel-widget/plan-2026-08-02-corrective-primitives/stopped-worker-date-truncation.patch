diff --git a/src/logic/propertyValueCoercion.ts b/src/logic/propertyValueCoercion.ts
index 9e662e8c..3dbc7eec 100644
--- a/src/logic/propertyValueCoercion.ts
+++ b/src/logic/propertyValueCoercion.ts
@@ -315,13 +315,13 @@ export function convertPropertyValueType(
 			return serialized ? [serialized] : [];
 		}
 		case 'date': {
-			if (Array.isArray(value)) return String(value[0] ?? '');
+			if (Array.isArray(value)) {
+				return convertPropertyValueType(value[0] ?? '', 'date');
+			}
 			const str = String((value as string | number | boolean) ?? '');
-			const dateMatch = str.match(
-				/(\d{4}-\d{2}-\d{2})(?:T|\s)?(\d{2}:\d{2}:\d{2})?/,
-			);
+			const dateMatch = str.match(/(\d{4}-\d{2}-\d{2})/);
 			if (dateMatch) {
-				return dateMatch[2] ? `${dateMatch[1]}T${dateMatch[2]}` : dateMatch[1];
+				return dateMatch[1];
 			}
 			return str;
 		}
